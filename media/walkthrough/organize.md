# Folders and tags

A chat lives in exactly one folder (its home) and can carry any number of tags.

- New Folder accepts a slash name like Work/ClientA to create a nested chain.
- New Tag creates a reusable label.
- Drag a chat onto a folder to move its home, or onto a tag to add the tag.
- Link a chat under another to nest it as a child in the Folders view.

All of this is your own organization layer. It is stored separately from your
transcripts and synced best-effort across machines.
